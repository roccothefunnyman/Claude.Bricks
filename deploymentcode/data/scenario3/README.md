# Scenario 3: Reference Models

Place reference .ldr files here for pattern extraction.

These will be analyzed by `scripts/scenario3/extract_stats.py` to produce
part-usage statistics, which are then clustered to discover building patterns.
